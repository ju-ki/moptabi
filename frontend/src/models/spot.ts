/**
 * スポット型定義 — shared-types/spot ドメインの re-export
 */
export {
  OpeningHoursSchema,
  PlanSpotSchema,
  SpotMetaSchema,
  SpotSchema,
  SpotWithMetaSchema,
} from '@shared/spot/schema';

export type { OpeningHoursType, PlanSpotType, SpotMetaType, SpotType, SpotWithMetaType } from '@shared/spot/types';
